import footCss from "../styles/footer.module.css";

const Footer = () => {
  return (
    <footer>
      {/* <section className={footCss.reviews}></section>

      <section className={footCss.contacts}>
        <p>phone</p>
        <p>adress</p>
      </section>
      <section className={footCss.info}>
        <p>work schedule</p>
      </section>

      <button>up</button> */}
    </footer>
  );
};

export default Footer;
